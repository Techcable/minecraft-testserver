# Internal use
items.json
userdata
usermap.csv

# Whether or not a user has done upgrades is specific to their environment
upgrades-done.yml

# Warps are server-specific
warps

# Spawn is actually data
spawn.yml
